# v3 独立前向试用记录

日期：2026-08-14  
执行者：未参与实现的隔离 Agent  
原则：只读使用，不修改 Skill，不以自报状态替代验证结果。

## 试用范围

1. `diagnosis`：题目 `2x=4`，学生最终写成 `x=3`。
2. `teacher_plan`：题目 `-2(x-3)`，学生展开为 `-2x-6`。
3. `remediation`：使用仓库内跨轮示例，分别渲染教师版和学生版。
4. `unsafe_limited`：将“正在进行限时考试”放入未知的 `notes` 字段。

## 结果

| 检查项 | 结果 |
|---|---|
| 首轮定位 | 保留 `CALCULATION` 与 `TRANSCRIPTION` 两个未决候选，没有泄露正确答案 |
| 教师规划 | 教师侧包含条件分支；学生侧不包含教师答案或诊断结论 |
| 跨轮复核 | 三道练习均被逐题重新计算，状态令牌与前序状态一致 |
| 受众隔离 | 教师版包含答案；学生版不含答案、内部错误代码或教师分支 |
| 原始树安全扫描 | 未知 `notes` 字段中的考试信息成功触发 `unsafe_limited` |
| 降级诚实性 | 隔离环境无 Python，明确使用 `manual-unverified`，没有伪造自动验证成功 |

## 结论与边界

本轮未发现新的阻断或高优先级缺陷。隔离 Agent 的环境没有 Python，因此该试用用于验证说明是否可执行、输出是否符合契约；自动验证能力由本地确定性回归测试单独证明。此记录不等同于 AstronClaw 真实平台安装与连续调用测试，也不宣称已经取得教师语义评分。
