# 历史 v1 的本地库。
dev/lib/v1/data/

# 历史 v3：对话数据(JSONL)与含密钥的配置。
dev/lib/v3/data/
dev/lib/v3/config.json

# 当前版本：App 持久状态、Kernel 临时状态与含密钥的配置。
var/
run/
etc/config.json

# 可选的本地启动环境变量。
.env
.env.*
!.env.example

# 依赖与构建产物
node_modules/
*.tsbuildinfo
dev/lib/v1/ui/dist/
dist/

# 系统与编辑器文件
.DS_Store
*.log

# 评测用的 venv(有些题要 pytest)。跑 eval 前:
#   python3 -m venv eval/.venv && eval/.venv/bin/pip install pytest
eval/.venv/
__pycache__/
